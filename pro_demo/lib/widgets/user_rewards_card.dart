import 'package:flutter/material.dart';

class UserRewardsCard extends StatelessWidget {
  const UserRewardsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}